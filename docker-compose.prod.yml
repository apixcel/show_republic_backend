services:
  # HTTP Gateway Service
  http-gateway:
    build:
      context: .
      dockerfile: ./apps/http-gateway/Dockerfile
    container_name: http-gateway-service
    volumes:
      - ./apps/http-gateway:/var/www/apps/http-gateway:cached
      - ./libs:/var/www/libs/:cached

    env_file:
      - .env
    ports:
      - '3000:3000'
    networks:
      - app-network
    restart: always
    environment:
      NODE_OPTIONS: '--max-old-space-size=4096'
    healthcheck:
      test: ['CMD', 'curl', '-f', 'http://http-gateway-service:3000/health']
      interval: 30s
      retries: 5
      start_period: 20s
      timeout: 10s

  # mongo:
  #   image: mongo
  #   container_name: mongo
  #   restart: always
  #   environment:
  #     MONGO_INITDB_ROOT_USERNAME: root
  #     MONGO_INITDB_ROOT_PASSWORD: root
  #     CLIENT_URL: mongodb://root:root@mongo:27017/show_republic?authSource=admin
  #   ports:
  #     - 27017:27017

  authentication:
    build:
      context: .
      dockerfile: ./apps/authentication/Dockerfile
    container_name: authentication
    volumes:
      - ./apps/authentication:/var/www/apps/authentication:cached
      - ./libs:/var/www/libs/:cached
    env_file:
      - .env
    ports:
      - '4000:3000'
    environment:
      NODE_OPTIONS: '--max-old-space-size=4096'
    depends_on:
      - nats
      - postgresdb
    networks:
      - app-network
    restart: always
    healthcheck:
      test: ['CMD', 'curl', '-f', 'http://authentication:3000/health']
      interval: 30s
      retries: 5
      start_period: 30s
      timeout: 10s

  # Posts Service
  posts:
    build:
      context: .
      dockerfile: ./apps/posts/Dockerfile
    container_name: posts
    volumes:
      - ./apps/posts:/var/www/apps/posts
      - ./libs:/var/www/libs/
    env_file:
      - .env
    ports:
      - '7001:3000'
    environment:
      NODE_ENV: development
    depends_on:
      - nats
      - postgresdb
    networks:
      - app-network
    restart: unless-stopped
    healthcheck:
      test: ['CMD', 'curl', '-f', 'http://posts:3000/health']
      interval: 30s
      retries: 5
      start_period: 30s
      timeout: 10s

  # gamefication service
  gamefication:
    build:
      context: .
      dockerfile: ./apps/gamefication/Dockerfile
    container_name: gamefication
    volumes:
      - ./apps/gamefication:/var/www/apps/gamefication
      - ./libs:/var/www/libs/
    env_file:
      - .env
    ports:
      - '8350:3000'
    environment:
      NODE_ENV: development
    depends_on:
      - nats
    networks:
      - app-network
    restart: unless-stopped
    healthcheck:
      test: ['CMD', 'curl', '-f', 'http://posts:3000/health']
      interval: 30s
      retries: 5
      start_period: 30s
      timeout: 10s

  # creator service
  creator:
    build:
      context: .
      dockerfile: ./apps/creator/Dockerfile
    container_name: creator
    volumes:
      - ./apps/creator:/var/www/apps/creator
      - ./libs:/var/www/libs/
    env_file:
      - .env
    ports:
      - '6900:3000'
    environment:
      NODE_ENV: development
    depends_on:
      - nats
      - postgresdb
    networks:
      - app-network
    restart: unless-stopped
    healthcheck:
      test: ['CMD', 'curl', '-f', 'http://posts:3000/health']
      interval: 30s
      retries: 5
      start_period: 30s
      timeout: 10s

  # category
  category:
    build:
      context: .
      dockerfile: ./apps/category/Dockerfile
    container_name: category
    volumes:
      - ./apps/category:/var/www/apps/category
      - ./libs:/var/www/libs/
    env_file:
      - .env
    ports:
      - '2000:3000'
    environment:
      NODE_ENV: development
    depends_on:
      - nats
      - postgresdb
    networks:
      - app-network
    restart: unless-stopped
    healthcheck:
      test: ['CMD', 'curl', '-f', 'http://category:3000/health']
      interval: 30s
      retries: 5
      start_period: 30s
      timeout: 10s
  product:
    build:
      context: .
      dockerfile: ./apps/product/Dockerfile
    container_name: product
    volumes:
      - ./apps/product:/var/www/apps/product
      - ./libs:/var/www/libs/
    env_file:
      - .env
    ports:
      - '2258:3000'
    environment:
      NODE_ENV: development
    depends_on:
      - nats
      - postgresdb
    networks:
      - app-network
    restart: unless-stopped
    healthcheck:
      test: ['CMD', 'curl', '-f', 'http://product:3000/health']
      interval: 30s
      retries: 5
      start_period: 30s
      timeout: 10s
  uploadfile:
    build:
      context: .
      dockerfile: ./apps/uploadfile/Dockerfile
    container_name: uploadfile
    volumes:
      - ./apps/uploadfile:/var/www/apps/uploadfile
      - ./libs:/var/www/libs/
    env_file:
      - .env
    ports:
      - '2269:3000'
    environment:
      NODE_ENV: development
    depends_on:
      - nats
      - postgresdb
    networks:
      - app-network
    restart: unless-stopped
    healthcheck:
      test: ['CMD', 'curl', '-f', 'http://uploadfile:3000/health']
      interval: 30s
      retries: 5
      start_period: 30s
      timeout: 10s

  # category
  statistics:
    build:
      context: .
      dockerfile: ./apps/statistics/Dockerfile
    container_name: statistics
    volumes:
      - ./apps/statistics:/var/www/apps/statistics
      - ./libs:/var/www/libs/
    env_file:
      - .env
    ports:
      - '8500:3000'
    environment:
      NODE_ENV: development
    depends_on:
      - nats
      - postgresdb
    networks:
      - app-network
    restart: unless-stopped
    healthcheck:
      test: ['CMD', 'curl', '-f', 'http://statistics:3000/health']
      interval: 30s
      retries: 5
      start_period: 30s
      timeout: 10s

  # admin service
  admin:
    build:
      context: .
      dockerfile: ./apps/admin/Dockerfile
    container_name: admin
    volumes:
      - ./apps/admin:/var/www/apps/admin
      - ./libs:/var/www/libs/
    env_file:
      - .env
    ports:
      - '2025:3000'
    environment:
      NODE_ENV: development
    depends_on:
      - nats
      - postgresdb
    networks:
      - app-network
    restart: unless-stopped
    healthcheck:
      test: ['CMD', 'curl', '-f', 'http://admin:3000/health']
      interval: 30s
      retries: 5
      start_period: 30s
      timeout: 10s
  role-permission:
    build:
      context: .
      dockerfile: ./apps/role-permission/Dockerfile
    container_name: role-permission
    volumes:
      - ./apps/role-permission:/var/www/apps/role-permission
      - ./libs:/var/www/libs/
    env_file:
      - .env
    ports:
      - '2135:3000'
    environment:
      NODE_ENV: development
    depends_on:
      - nats
      - postgresdb
    networks:
      - app-network
    restart: unless-stopped
    healthcheck:
      test: ['CMD', 'curl', '-f', 'http://role-permission:3000/health']
      interval: 30s
      retries: 5
      start_period: 30s
      timeout: 10s

  your-channel:
    build:
      context: .
      dockerfile: ./apps/your-channel/Dockerfile
    container_name: your-channel
    volumes:
      - ./apps/your-channel:/var/www/apps/your-channel
      - ./libs:/var/www/libs/
    env_file:
      - .env
    ports:
      - '2137:3000'
    environment:
      NODE_ENV: development
    depends_on:
      - nats
      - postgresdb
    networks:
      - app-network
    restart: unless-stopped
    healthcheck:
      test: ['CMD', 'curl', '-f', 'http://your-channel:3000/health']
      interval: 30s
      retries: 5
      start_period: 30s
      timeout: 10s

  subscription:
    build:
      context: .
      dockerfile: ./apps/subscription/Dockerfile
    container_name: subscription
    volumes:
      - ./apps/subscription:/var/www/apps/subscription
      - ./libs:/var/www/libs/
    env_file:
      - .env
    ports:
      - '2139:3000'
    environment:
      NODE_ENV: development
    depends_on:
      - nats
      - postgresdb
    networks:
      - app-network
    restart: unless-stopped
    healthcheck:
      test: ['CMD', 'curl', '-f', 'http://subscription:3000/health']
      interval: 30s
      retries: 5
      start_period: 30s
      timeout: 10s

  settings:
    build:
      context: .
      dockerfile: ./apps/settings/Dockerfile
    container_name: settings
    volumes:
      - ./apps/settings:/var/www/apps/settings
      - ./libs:/var/www/libs/
    env_file:
      - .env
    ports:
      - '2141:3000'
    environment:
      NODE_ENV: development
    depends_on:
      - nats
      - postgresdb
    networks:
      - app-network
    restart: unless-stopped
    healthcheck:
      test: ['CMD', 'curl', '-f', 'http://settings:3000/health']
      interval: 30s
      retries: 5
      start_period: 30s
      timeout: 10s

  live:
    build:
      context: .
      dockerfile: ./apps/live/Dockerfile
    container_name: live
    volumes:
      - ./apps/live:/var/www/apps/live
      - ./libs:/var/www/libs/
    env_file:
      - .env
    ports:
      - '2142:3000'
    environment:
      NODE_ENV: development
    depends_on:
      - nats
      - postgresdb
    networks:
      - app-network
    restart: unless-stopped
    healthcheck:
      test: ['CMD', 'curl', '-f', 'http://live:3000/health']
      interval: 30s
      retries: 5
      start_period: 30s
      timeout: 10s

  wallet:
    build:
      context: .
      dockerfile: ./apps/wallet/Dockerfile
    container_name: wallet
    volumes:
      - ./apps/wallet:/var/www/apps/wallet
      - ./libs:/var/www/libs/
    env_file:
      - .env
    ports:
      - '2143:3000'
    environment:
      NODE_ENV: development
    depends_on:
      - nats
      - postgresdb
    networks:
      - app-network
    restart: unless-stopped
    healthcheck:
      test: ['CMD', 'curl', '-f', 'http://wallet:3000/health']
      interval: 30s
      retries: 5
      start_period: 30s
      timeout: 10s

  brand:
    build:
      context: .
      dockerfile: ./apps/brand/Dockerfile
    container_name: brand
    volumes:
      - ./apps/brand:/var/www/apps/brand
      - ./libs:/var/www/libs/
    env_file:
      - .env
    ports:
      - '2144:3000'
    environment:
      NODE_ENV: development
    depends_on:
      - nats
      - postgresdb
    networks:
      - app-network
    restart: unless-stopped
    healthcheck:
      test: ['CMD', 'curl', '-f', 'http://brand:3000/health']
      interval: 30s
      retries: 5
      start_period: 30s
      timeout: 10s

  promotion:
    build:
      context: .
      dockerfile: ./apps/promotion/Dockerfile
    container_name: promotion
    volumes:
      - ./apps/promotion:/var/www/apps/promotion
      - ./libs:/var/www/libs/
    env_file:
      - .env
    ports:
      - '2145:3000'
    environment:
      NODE_ENV: development
    depends_on:
      - nats
      - postgresdb
    networks:
      - app-network
    restart: unless-stopped
    healthcheck:
      test: ['CMD', 'curl', '-f', 'http://promotion:3000/health']
      interval: 30s
      retries: 5
      start_period: 30s
      timeout: 10s

  profile:
    build:
      context: .
      dockerfile: ./apps/profile/Dockerfile
    container_name: profile
    volumes:
      - ./apps/profile:/var/www/apps/profile
      - ./libs:/var/www/libs/
    env_file:
      - .env
    ports:
      - '2138:3000'
    environment:
      NODE_ENV: development
    depends_on:
      - nats
      - postgresdb
    networks:
      - app-network
    restart: unless-stopped
    healthcheck:
      test: ['CMD', 'curl', '-f', 'http://profile:3000/health']
      interval: 30s
      retries: 5
      start_period: 30s
      timeout: 10s

  # postComment
  post-comment:
    build:
      context: .
      dockerfile: ./apps/post-comment/Dockerfile
    container_name: post-comment
    volumes:
      - ./apps/post-comment:/var/www/apps/post-comment
      - ./libs:/var/www/libs/
    env_file:
      - .env
    ports:
      - '4200:3000'
    environment:
      NODE_ENV: development
    depends_on:
      - nats
      - postgresdb
    networks:
      - app-network
    restart: unless-stopped
    healthcheck:
      test: ['CMD', 'curl', '-f', 'http://post-comment:3000/health']
      interval: 30s
      retries: 5
      start_period: 30s
      timeout: 10s

  notification:
    build:
      context: .
      dockerfile: ./apps/notification/Dockerfile
    container_name: notification
    volumes:
      - ./apps/notification:/var/www/apps/notification
      - ./libs:/var/www/libs/
    env_file:
      - .env
    ports:
      - '4400:3000'
    environment:
      NODE_ENV: development
    depends_on:
      - nats
      - postgresdb
    networks:
      - app-network
    restart: unless-stopped
    healthcheck:
      test: ['CMD', 'curl', '-f', 'http://notification:3000/health']
      interval: 30s
      retries: 5
      start_period: 30s
      timeout: 10s

  # postLikeToggle
  post-like-toggle:
    build:
      context: .
      dockerfile: ./apps/post-like-toggle/Dockerfile
    container_name: post-like-toggle
    volumes:
      - ./apps/post-like-toggle:/var/www/apps/post-like-toggle
      - ./libs:/var/www/libs/
    env_file:
      - .env
    ports:
      - '4300:3000'
    environment:
      NODE_ENV: development
    depends_on:
      - nats
      # - mongo
    networks:
      - app-network
    restart: unless-stopped
    healthcheck:
      test: ['CMD', 'curl', '-f', 'http://post-like-toggle:3000/health']
      interval: 30s
      retries: 5
      start_period: 30s
      timeout: 10s

  nats:
    image: nats
    container_name: nats-server
    ports:
      - '4222:4222'
      - '8222:8222'
    networks:
      - app-network
    restart: unless-stopped
    healthcheck:
      test: ['CMD', 'curl', '-f', 'http://localhost-server:8222/']
      interval: 30s
      retries: 5
      start_period: 30s
      timeout: 10s

  # PostgreSQL Database
  postgresdb:
    image: postgres:13
    container_name: postgresdb
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: qubitars
      POSTGRES_DB: demo
    ports:
      - '5433:5432'
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - app-network
    restart: unless-stopped
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U postgres']
      interval: 30s
      retries: 5
      start_period: 30s
      timeout: 10s

  # pgAdmin
  pgadmin:
    image: dpage/pgadmin4
    container_name: pgadmin
    environment:
      PGADMIN_DEFAULT_EMAIL: hammadakram6223@gmail.com
      PGADMIN_DEFAULT_PASSWORD: qubitars
    ports:
      - '5050:80'
    depends_on:
      - postgresdb
    networks:
      - app-network
    restart: unless-stopped
    healthcheck:
      test: ['CMD', 'curl', '-f', 'http://pgadmin:80/']
      interval: 30s
      retries: 5
      start_period: 30s
      timeout: 10s

networks:
  app-network:
    driver: bridge

volumes:
  postgres_data:
